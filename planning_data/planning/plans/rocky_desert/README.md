# Rocky Desert plan data

This directory contains Rocky Desert geography/resource-assignment plan inputs.

Player/build-strategy choices were moved to:

`planning/strategies/rocky_relaxed_districted_v1/`

The planner engine vocabulary/types live under `planning/model/`.
